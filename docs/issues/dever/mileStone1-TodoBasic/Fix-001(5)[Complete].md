**[Assignee]:** @AI_Dever

**[Issue]:** #Fix-001(5) (왼쪽 부분 짧아지는 효과 및 떨림 효과 구현)

**[Feature Name]:** 스와이프/선택 시 왼쪽 부분이 짧아지고 미세하게 떨리는 시각적 효과

**[Milestone]:** Milestone 1: 기본 ToDo CRUD 기능

**[Parent Issue]:** Fix-001 (삭제 기능 UI/UX 개선)

---

## 📖 기능 개요

### 목적
스와이프 중이거나 선택된 할 일에 왼쪽 부분이 짧아지는 효과를 추가하고, 미세한 떨림 효과를 구현합니다. 마치 '어떤 구역 밖으로 밀려나서 사라지고 있는' 느낌을 주어 사용자에게 직관적인 피드백을 제공합니다.

### 사용자 스토리
- **As a** 사용자
- **I want** 스와이프하거나 선택된 할 일이 왼쪽 부분이 짧아지고 미세하게 떨리는 효과
- **So that** 스와이프 동작이 더 직관적이고 삭제될 것임을 명확히 알 수 있다

---

## 🎯 기능 요구사항

### 핵심 기능
1. 스와이프 중인 할 일에 왼쪽 부분이 짧아지는 효과
2. 선택된 할 일에도 왼쪽 부분이 짧아지는 효과 (밀려난 정도만큼)
3. 스와이프 중이거나 선택된 할 일에 미세한 떨림 효과

### 상세 요구사항
- **왼쪽 부분 짧아지는 효과**:
  - 스와이프 중인 할 일: 왼쪽으로 -30px 밀려났다면, 왼쪽 부분이 30px 정도 짧아진 느낌
  - 선택된 할 일: 선택 상태로 밀려난 정도(-40px)만큼 왼쪽 부분이 짧아짐
  - `clip-path` 또는 `overflow: hidden` + `transform: translateX` 조합 사용
  - 밀려난 정도에 비례하여 왼쪽에서 잘리는 부분 증가
- **미세한 떨림 효과**:
  - 스와이프 중이거나 선택된 할 일에 미세한 떨림 애니메이션 적용
  - `animation: shake` 또는 `transform: translateX`를 이용한 미세한 진동
- **시각적 효과**: 마치 구역 밖으로 밀려나서 사라지는 느낌
- **실시간 업데이트**: 스와이프 중 실시간으로 잘리는 정도 업데이트

---

## 🎨 UI/UX 명세

### 화면 구성
- 스와이프 중인 할 일: 왼쪽으로 밀려나면서 왼쪽 부분이 점점 짧아지고 미세하게 떨림
- 선택된 할 일: 왼쪽으로 밀려난 정도만큼 왼쪽 부분이 짧아지고 미세하게 떨림
- 스와이프/선택 정도가 많을수록 더 많이 잘림

### 인터랙션
- 왼쪽 스와이프 시작 → 왼쪽 부분이 짧아지기 시작하고 떨림 시작
- 스와이프 진행 → 왼쪽 부분이 점점 짧아지고 떨림 유지
- 스와이프 완료 → 선택 상태로 전환, 선택된 정도만큼 왼쪽 부분이 짧아지고 떨림 유지

### 애니메이션
- 스와이프 중 실시간으로 잘리는 정도 업데이트
- 미세한 떨림 애니메이션 지속
- 부드러운 전환 효과

---

## 🔗 관련 이슈

- **부모 이슈**: Fix-001 (삭제 기능 UI/UX 개선)
- **이전 단계**: Fix-001(4) (하단 선택 바 구현)

---

## 📝 작업 내역

### [날짜] - 작업 시작
- 이슈 생성 및 요구사항 정의

---

## 작업 완료 내역

### 변경 사항
- `src/lib/components/PlanningMode.svelte`: 왼쪽 부분 짧아지는 효과 및 떨림 효과 구현

### 주요 수정 사항
1. **li 요소에 조건부 클래스 추가**
   - `class:deleting={isSwiping && swipingId === task.id || isSelected(task.id)}` 추가
   - 스와이프 중이거나 선택된 할 일에 `deleting` 클래스 적용

2. **왼쪽 부분 짧아지는 효과 구현**
   - 스와이프 중인 할 일: `clip-path: inset(0 0 0 ${clipLeft}px)` 사용
     - 왼쪽으로 밀려난 정도(`swipeAmount`)만큼 왼쪽에서 잘림
     - 최대 100px까지 제한
   - 선택된 할 일: `clip-path: inset(0 0 0 40px)` 적용
     - 선택 상태로 밀려난 정도(-40px)만큼 왼쪽 부분이 짧아짐
   - `overflow: hidden` 적용하여 잘린 부분이 보이지 않도록
   - 스와이프 정도에 비례하여 실시간으로 업데이트

3. **미세한 떨림 효과 구현**
   - JavaScript로 떨림 효과 계산 (`getVibrateOffset()`)
   - `requestAnimationFrame`을 사용한 부드러운 애니메이션
   - `Math.sin(time * 50) * 0.5`로 미세한 진동 (-0.5px ~ 0.5px)
   - 스와이프 중이거나 선택된 할 일에만 적용
   - `transform: translateX(calc(... + ${vibrateOffset}px))`로 transform에 포함

4. **스타일 속성 동적 적용**
   - 스와이프 중일 때: `transform: translateX(calc(${swipeX}px + ${vibrateOffset}px)); clip-path: inset(0 0 0 ${clipLeft}px);`
   - 선택된 상태일 때: `transform: translateX(calc(-40px + ${vibrateOffset}px)); clip-path: inset(0 0 0 40px);`
   - 왼쪽으로 스와이프한 경우에만 `clip-path` 적용

5. **CSS 스타일 추가**
   - `li.deleting`: `overflow: hidden`, `will-change: transform, clip-path`
   - `li.selected`: 선택된 할 일의 시각적 효과 (opacity, background, border)

6. **애니메이션 프레임 관리**
   - `startVibrate()`: 떨림 효과 시작
   - `stopVibrate()`: 떨림 효과 중지
   - 반응형 문(`$:`)으로 스와이프/선택 상태 변경 시 자동 시작/중지
   - `onDestroy`에서 애니메이션 프레임 정리

### 테스트 결과
- ✅ 스와이프 중인 할 일에 왼쪽 부분이 짧아지는 효과 확인
- ✅ 선택된 할 일에도 왼쪽 부분이 짧아지는 효과 확인
- ✅ 스와이프 중이거나 선택된 할 일에 미세한 떨림 효과 확인
- ✅ 스와이프 정도에 비례하여 잘리는 정도 조절 확인
- ✅ 스와이프 중 실시간으로 잘리는 정도 업데이트 확인
- ✅ 부드러운 전환 효과 확인

### 코드 변경 내역
- **추가된 코드**:
  - `li` 요소에 `class:deleting` 조건부 클래스
  - 스와이프 정도 계산 로직 (clip-path 적용)
  - 떨림 효과 계산 함수 (`getVibrateOffset`, `startVibrate`, `stopVibrate`)
  - `vibrateOffset` 변수 및 `vibrateAnimationFrame` 관리
  - 동적 `style` 속성에 `clip-path` 및 떨림 효과 적용
  - `li.deleting` CSS 클래스 스타일
  - `onDestroy`에 애니메이션 프레임 정리 코드

---

**작성일**: 2024년 12월 1일  
**마지막 업데이트**: 2024년 12월 1일  
**완료일**: 2024년 12월 1일
