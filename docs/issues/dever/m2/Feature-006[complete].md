**[Assignee]:** @AI_Dever

**[Issue]:** #Feature-006 (Supabase 클라이언트 설정 및 기본 연동)

**[Feature Name]:** Supabase 클라이언트 라이브러리 통합 및 기본 설정

**[Milestone]:** Milestone 2: 환경 구축 및 서버 연동 (Phase 0)

---

## 📖 기능 개요

### 목적
Supabase 클라이언트 라이브러리를 프로젝트에 통합하고 기본 설정을 완료합니다. 이후 인증 및 데이터베이스 접근 기능의 기반을 마련합니다.

### 사용자 스토리
- **As a** 개발자
- **I want** Supabase 클라이언트를 설정하고 연동하는 것
- **So that** 인증 및 데이터베이스 기능을 구현할 수 있다

---

## 🎯 기능 요구사항

### 핵심 기능
1. Supabase 클라이언트 라이브러리 설치
2. Supabase 클라이언트 생성 및 설정
3. 환경 변수 설정
4. Supabase 작동 방식 이해 및 문서화

### 상세 요구사항
- **라이브러리 설치**:
  - `@supabase/supabase-js` 패키지 설치
  - `package.json`에 의존성 추가
- **클라이언트 생성**:
  - `src/lib/supabaseClient.js` 파일 생성
  - Supabase 클라이언트 인스턴스 생성 및 export
- **환경 변수 설정**:
  - `.env` 파일에 `VITE_SUPABASE_URL` 추가
  - `.env` 파일에 `VITE_SUPABASE_ANON_KEY` 추가
  - 환경 변수 로드 확인
- **Supabase 작동 방식 이해**:
  - BaaS 개념 이해
  - 클라이언트에서 직접 DB 접근 가능함을 확인
  - 서버 구현 불필요함을 문서화

---

## 🎨 UI/UX 명세

### 화면 구성
- 이 단계에서는 UI 구현이 없습니다. 클라이언트 설정만 완료합니다.

### 인터랙션
- 이 단계에서는 사용자 인터랙션이 없습니다.

### 애니메이션
- 이 단계에서는 애니메이션이 없습니다.

---

## 🔧 기술 명세

### Supabase 작동 방식 (서버 구현 불필요)

**핵심 개념**: Supabase는 BaaS(Backend as a Service)로, 클라우드에서 제공하는 데이터베이스와 인증 서비스를 사용합니다.

#### 데이터베이스 접근 방식
- **Supabase가 서버 역할**: Supabase가 클라우드에서 PostgreSQL 데이터베이스를 호스팅합니다
- **클라이언트에서 직접 접근**: 클라이언트에서 Supabase 클라이언트 라이브러리를 통해 직접 DB에 접근합니다
- **RLS로 보안 관리**: Row Level Security 정책으로 사용자별 데이터 분리를 보장합니다
- **별도 서버 불필요**: Express/Node.js 서버를 구축할 필요가 없습니다

#### 데이터 흐름
```
클라이언트 (브라우저)
    ↓
Supabase 클라이언트 라이브러리 (@supabase/supabase-js)
    ↓
Supabase 클라우드 서버 (PostgreSQL DB + Auth 서버)
    ↓
데이터 반환
```

#### 예시: 데이터 조회
```javascript
// 클라이언트에서 직접 DB 조회 (서버 불필요)
import { supabase } from '$lib/supabaseClient';

// 로그인한 사용자의 Tasks만 조회 (RLS 정책으로 자동 필터링)
const { data, error } = await supabase
  .from('tasks')
  .select('*')
  .eq('user_id', user.id);
```

### 구현 방법
**중요**: 별도의 서버 구현이 필요 없습니다. Supabase는 BaaS(Backend as a Service)이므로 클라이언트 사이드에서만 구현 가능합니다.

#### 1. Supabase 클라이언트 생성
```javascript
// src/lib/supabaseClient.js
import { createClient } from '@supabase/supabase-js';

const supabaseUrl = import.meta.env.VITE_SUPABASE_URL;
const supabaseAnonKey = import.meta.env.VITE_SUPABASE_ANON_KEY;

export const supabase = createClient(supabaseUrl, supabaseAnonKey);
```

### 테스트 방법
- Supabase 클라이언트가 정상적으로 생성되는지 확인
- 환경 변수가 올바르게 로드되는지 확인
- 클라이언트 인스턴스를 다른 파일에서 import하여 사용 가능한지 확인

---

## 🔗 관련 이슈

- **부모 이슈**: Milestone 2 (환경 구축 및 서버 연동)
- **의존 이슈**: 
  - M2.2: Supabase DB 초기 설정 (Supabase 프로젝트 생성 필요)
- **후속 이슈**: 
  - Feature-007: Google OAuth 설정 및 로그인 UI
  - Feature-008: 인증 로직 및 세션 관리
  - Feature-009: 인증 기반 서비스 활성화

---

## 📝 작업 내역

### [날짜] - 작업 시작
- 이슈 생성 및 요구사항 정의

### [2024-12-01] - 구현 완료
- ✅ `@supabase/supabase-js` 패키지 확인 (이미 설치됨)
- ✅ `src/lib/supabaseClient.js` 파일 생성 및 클라이언트 설정 완료
- ✅ 환경 변수 검증 로직 추가 (환경 변수 미설정 시 경고 메시지)
- ✅ `.env.example` 파일 생성 (환경 변수 템플릿)
- ✅ Supabase 작동 방식 문서화 (BaaS 개념, 서버 불필요 설명)

---

**참고 문서**:
- `docs/brainstorm.md` (942-966줄): Google 로그인 구현 예시
- `docs/devGuideLine.md`: Supabase 사용 가이드
- `docs/Milestone-Basic(Detail).md`: Milestone 2 상세 내용
- Supabase 공식 문서: https://supabase.com/docs/guides/auth

**작성일**: 2024년  
**마지막 업데이트**: 2024년

---

## 추가 작업 필요 내용

### 사용자가 직접 수행해야 할 작업

#### 1. Supabase 프로젝트 생성
- [ ] Supabase 계정 생성 (https://app.supabase.com)
- [ ] 새 프로젝트 생성
- [ ] 프로젝트 URL 및 API 키 확인

#### 2. 환경 변수 설정
- [ ] 프로젝트 루트에 `.env` 파일 생성 (`.env.example` 참고)
- [ ] `.env` 파일에 실제 Supabase 프로젝트 정보 입력:
  ```
  VITE_SUPABASE_URL=https://your-actual-project-id.supabase.co
  VITE_SUPABASE_ANON_KEY=your-actual-anon-key
  ```
- [ ] `.env` 파일이 `.gitignore`에 포함되어 있는지 확인 (이미 설정됨)

#### 3. Supabase 데이터베이스 초기 설정 (M2.2 의존)
- [ ] Supabase 대시보드에서 데이터베이스 스키마 설계
- [ ] `tasks` 테이블 생성 (필요한 경우)
- [ ] RLS(Row Level Security) 정책 설정
- [ ] 관련 이슈: `docs/issues/dever/m2/Feature-M2-01.md` (Supabase DB 초기 설정)

#### 4. 테스트 및 검증
- [ ] 개발 서버 실행 (`npm run dev`)
- [ ] 브라우저 콘솔에서 환경 변수 경고 메시지 확인
- [ ] `.env` 파일 설정 후 경고 메시지가 사라지는지 확인
- [ ] `supabaseClient.js`를 다른 파일에서 import하여 사용 가능한지 확인

#### 5. 다음 단계 준비
- [ ] Feature-007: Google OAuth 설정 및 로그인 UI 구현 준비
- [ ] Feature-008: 인증 로직 및 세션 관리 구현 준비
